# 期末项目依赖清单
# 安装方式：python -m pip install -r requirements.txt

# ===== 数据处理 =====
pandas==3.0.3            # DataFrame 操作、清洗、RFM 计算
numpy==2.4.5             # 数值计算（log 轴 bins、log10）
openpyxl==3.1.5          # pandas 读取 .xlsx 文件的隐式依赖（不装会报错）

# ===== 可视化 =====
matplotlib==3.10.9       # 8 张图表的画图引擎
seaborn==0.13.2          # 周×小时热力图

# ===== GUI =====
pillow==12.2.0           # 图片显示（缩略图、大图预览、CTkImage 底层）
customtkinter==5.2.2     # 现代 Tkinter UI 库（圆角、亮/暗主题、统一字体）

# ===== AI 解读 =====
openai==2.38.0           # OpenAI 兼容协议（同时支持 DeepSeek/Moonshot/智谱等）

# ===== 部分第三方库依赖已省略 =====